import PropTypes from 'prop-types';

const icons = PropTypes.shape({
   check: PropTypes.string,
   uncheck: PropTypes.string,
   halfCheck: PropTypes.string,
   expandClose: PropTypes.string,
   expandOpen: PropTypes.string,
   expandAll: PropTypes.string,
   collapseAll: PropTypes.string,
   parentClose: PropTypes.string,
   parentOpen: PropTypes.string,
   leaf: PropTypes.string
});

export default icons;
